import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  headline: {
    fontSize: 25,
    fontWeight: "bold",
    padding: 20,
  },
  list: {
    padding: 20,
  },
});

export default styles;
